name: master
on:
  push:
    branches:
      - master
      - main
jobs:
    master:
        runs-on: ubuntu-latest
        steps:
            - name: "Checkout code"
              run: echo "Checkout code"
